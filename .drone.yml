kind: pipeline
name: ci
steps:
- commands:
  - whoami
  - pwd
  - ls -ld
  - ls -l
  - make repo
  image: zachfi/shell:archlinux
  name: repo
  pull: always
  volumes:
  - name: cache
    path: /repo
- commands:
  - make image
  image: zachfi/shell:archlinux
  name: image
  pull: always
volumes:
- name: cache
  temp: {}
